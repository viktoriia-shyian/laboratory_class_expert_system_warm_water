<Window x:Class="WpfAppLR6_8.WindowInput"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppLR6_8"
        mc:Ignorable="d"
        Title="WindowInput" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="datagrid_facts" CanUserAddRows="True" AutoGenerateColumns="False" ItemsSource="{Binding FactCollection, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" >
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Id}"></DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding Value}"></DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding Description}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        
        
        
        
        <Button x:Name="check_working" Click="check_working_Click" Content="dfsjak"/>



    </Grid>
</Window>
